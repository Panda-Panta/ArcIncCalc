# 明日方舟基建收益预测软件：缺陷审计与实施计划

> **For agentic workers:** 实施时必须使用 `superpowers:test-driven-development`；阶段验收前必须使用 `superpowers:verification-before-completion`。涉及基建规则必须使用 `arknights-riic-mechanics`。

**目标：** 在保留现有 Mower 主排班编辑体验和无损导入导出的前提下，把“静态兼容投影 + 近似稳态计算”升级为可解释、可回归、能正确执行 Mower 主排班语义的长期基建产出模拟器。

**架构：** Mower JSON/JPG 适配层只负责无损序列化；排班编译器生成完整的有序候选、动态休息池和特殊换班意图；事件模拟器统一推进心情、换班、订单快照、无人机与库存；技能规则层输出结构化效果和覆盖率；UI 只消费验证与计算结果。

**技术栈：** Vue 3、TypeScript 5.9、Pinia、Vite/Vitest、Naive UI、jsQR、pako、html-to-image。

**规格依据：** `docs/superpowers/specs/2026-09-02-mower-workbench-compatibility-design.md`、`docs/用户说明书.md`、`docs/更新说明-2026-09-08.md`、`src/workbench/compat/fixtures/mower-252-2gold.json`。

## 全局约束

- 当前分支是 `mode/shift-run`，只实现跑单版本，不把常规版默认行为混入本分支。
- 副表、`backup_plans`、触发器 AST 和 `roomTask` 已被用户明确排除出当前核心；继续无损保存，但在核心产出闭环之后再实现执行语义。
- 不以“测试通过”替代数值正确性。真实 252 金标准、事件守恒和规则覆盖率必须同时通过。
- 不静默忽略未知技能、干员、房间或未执行的 Mower 字段；报告必须说明影响范围。
- 相同输入、数据版本、规则版本和模拟参数必须得到相同结果。
- UI 不直接拼装算法输入；所有转换由纯函数编译器完成，且不得修改输入对象。
- 每个规则修复先写失败测试，再写最小实现；不得通过改写期望值掩盖金标准失败。

## 一、当前状态与基线

### 已完成

| 能力 | 状态 | 证据 |
| --- | --- | --- |
| Mower 主排班 UI | 已有 980px 固定画布、核心设施卡、拖拽与替补编辑 | `src/components/workbench/` |
| JSON 互通 | 可往返顶层 `default`、主表、`conf`、`backup_plans` 与未知字段 | `src/workbench/compat/mowerJson.ts` |
| JPG 排班图 | 支持 16-QR、缩放图和 JPEG 有损压缩 | `src/workbench/compat/mowerQrCodec.ts` |
| 设施推断 | 2/3 电站等级规则已有独立实现和测试 | `src/workbench/levelInference.ts` |
| 中文干员显示 | 常见 ID/英文别名可解析为中文名 | `src/workbench/operatorHelpers.ts` |
| 特殊配置识别 | 已识别跑单替补、菲亚梅塔目标、宿管和 `Free` | `src/workbench/validate.ts` |
| 静态收益 | 制造、贸易、电力、无人机、部分技能和心情轮换可计算 | `src/engine/` |

### 已验证问题

- 真实 `252二赤金Test` 已能导入、通过工作台验证并进入计算桥。
- 当前测试会扫描 `.worktrees/mower-workbench`，使旧工作树测试被重复计入；测试数量不能作为可靠基线。
- 当前 252 结果与 Excel 第三个工作表仍有显著差距：作战记录约 `-16.3%`、赤金价值约 `-20.6%`、贸易龙门币约 `-15.0%`，综合口径约 `-17.8%`。
- 结论：文件与 UI 兼容基本成立，但动态排班、逐订单跑单和资源闭环尚未成立。

## 二、漏洞与不足

### P0：直接影响产出正确性

#### P0-1 有序多替补被降级为一对一

`src/workbench/adapter.ts` 把 `replacements: string[]` 至多投影为一名当前可用候选（跳过在岗或已经占用者）；`src/engine/shiftRoster.ts` 也只支持一个主力和一个替补。投影完成后其余候选全部丢失，运行期无法在状态变化后继续按原顺序选择。

**影响：** 长周期换班覆盖率、多人共享候选和主力回岗时间均可能错误。

#### P0-2 `Free` 只是过滤为空，不是动态休息池

工作台可显示 `Free`，但编译到旧引擎时被过滤。引擎没有宿舍床位队列、恢复速度、满心情离开和工作设施补位事件。

**影响：** 依赖弹性轮休的 Mower 主表会低估或错误估计有效工作时长。

#### P0-3 菲亚梅塔目标只被识别，未被执行

验证器允许菲亚梅塔在宿舍引用当前在岗主力，但目标列表没有进入旧计算。事件链应在菲亚梅塔满心情时按 Mower 策略重新筛选目标：目标心情变为 24，菲亚梅塔获得目标交换前的心情，并以固定 2 点/小时恢复；不能简化为固定归零、固定 12 小时或循环轮询。

**影响：** 252 等依靠菲亚梅塔维持核心干员心情的方案无法得到真实可持续效率。

#### P0-4 跑单是房间常量，不是订单快照

当前 `specialOrder: shiftRun` 直接改变整段贸易站期望。Mower 会在当前订单倒计时结束前换入跑单干员，但已有证据不足以证明品质、基础时长与特殊奖励在同一时点确定。新引擎须区分订单获取开始、获取完成和交付，并把时点口径作为显式假设；获取完成后的订单快照不随后续换班改写。

**影响：** 跑单角色会被错误地影响整段效率或所有订单，无法解释单笔订单。

#### P0-5 赤金、订单和仓储没有资源闭环

赤金制造、贸易消耗、初始库存、订单队列、仓库/订单上限、收取间隔和停产没有在同一账本推进。当前结果近似“原料无限且及时收取”的理论上限。

**影响：** 赤金不足时仍可能计入贸易收入，无法判断长期可持续性。

#### P0-6 固定预热/采样窗口无收敛证明

`src/engine/calculate.ts` 固定使用 30 天预热和 90 天采样，没有根据排班周期、库存周期或状态收敛自动决定，也未在报告中公开残差。

**影响：** 长周期或非整除周期可能受初始状态偏置。

#### P0-7 数据覆盖不等于规则覆盖

数据层有 425 份档案和 746 条最高阶段技能，但 `operatorRules.ts` 仍会输出 `unquantifiedSkills`。技能阶段默认取最高，缺少用户精英阶段选择；阵营、技能类别、多项联动和跨设施状态没有全量机器可读覆盖矩阵。

**影响：** 新干员或复杂联动可能仅显示文本，结果却看似精确。

### P1：可解释性、兼容性与体验

#### P1-1 两套验证口径分裂

工作台验证已使用中文名和 Mower 特殊语义；旧引擎仍要求每名主力都有唯一替补，并可能输出 `char_xxx`。桥接层只是在绕开冲突，根因是领域模型不同。

#### P1-2 UI 缺少真实截图回归

现有视觉测试主要检查 DOM、尺寸和滚动契约，不是像素/截图差异测试。1920×1080、1366×768、720×900、150% Windows 缩放、键盘焦点和小屏弹窗缺少真实浏览器验收。

#### P1-3 结果解释不足

报告有部分 `buffDetails`，但缺少统一公式树、事件时间线、资源守恒、假设、数据版本、规则版本和收敛信息，难以定位与 Excel/Mower 的差异。

#### P1-4 导入诊断与防护不完整

JSON/JPG 已有结构校验和扫描次数上限，但需补文件大小、图像像素数、解压文本大小、二维码重复/缺片、未知干员、版本不兼容的分级错误与恢复建议。

#### P1-5 数据更新依赖个人绝对路径

`pnpm audit:data` 写死开发机 GameData 路径。数据版本、规则覆盖率和更新差异无法在其他机器稳定复现。

#### P1-6 性能和包体无预算

干员全集、二维码和工作台进入前端主包。缺少首屏包体、懒加载、大图内存、长周期模拟和低配置机器基准。

#### P1-7 测试范围污染

`vite.config.ts` 没有排除 `.worktrees/**`。旧或重复测试会制造假阳性并延长验证时间。

#### P1-8 特殊占位的验证与编译口径不完全一致

部分验证路径会把 `Free` / `Current` 当作已占席位统计，编译器则会把它们过滤为空。特定占位组合可能先被容量校验阻断，或进入旧引擎后被静默清空。需要统一“配置席位、运行时人员、休息池占位”三种计数。

### P2：产品与维护

- 浏览器本地应用尚无正式桌面打包、迁移、备份和故障恢复验收。
- schema v7/v8 双模型并存；兼容层承担领域降级，容易出现“保存了但没执行”。
- 缺少完整金标准集：252 二赤金、252 三赤金、342 纯钱、342 搓玉、无菲亚梅塔、赤金不足。
- 缺少规则来源、可信度、适用版本和最后核验日期登记。
- 自动排班、布局优化和无人机最优分配尚未实现；应等待事件引擎正确后再做。
- `backup_plans`、触发器与 `roomTask` 仅无损保存是当前明确边界，不算核心漏洞；仅导出丢失时按兼容性缺陷处理。

## 三、目标架构

### 深模块接口

```ts
// src/scheduler/types.ts
export interface CompiledSchedule {
  rooms: CompiledRoom[]
  operators: Record<string, OperatorRuntimeState>
  restPools: RestPool[]
  policies: MainRosterPolicies
  runOrderPolicies: RunOrderPolicy[]
  assumptions: SimulationAssumptions
}

export interface CompiledSlot {
  roomId: string
  slotIndex: number
  primaryOperatorId: string | null
  orderedCandidates: string[]
  groupId: string | null
  role: 'work' | 'dorm-keeper' | 'free-rest' | 'fiammetta'
}
```

```ts
// src/simulator/types.ts
export type SimulationEvent =
  | { kind: 'morale-threshold'; at: number; operatorId: string }
  | { kind: 'roster-change'; at: number; roomId: string }
  | { kind: 'fiammetta-swap'; at: number; targetId: string }
  | { kind: 'order-generated'; at: number; roomId: string; snapshot: OrderSnapshot }
  | { kind: 'order-collected'; at: number; roomId: string; orderId: string }
  | { kind: 'drone-used'; at: number; targetId: string; amount: number }
  | { kind: 'inventory-boundary'; at: number; resource: ResourceKind }

export interface SimulationResult {
  report: CalculationReportV2
  events: SimulationEvent[]
  diagnostics: SimulationDiagnostic[]
  convergence: ConvergenceReport
}
```

```ts
// src/rules/coverage.ts
export interface RuleCoverageEntry {
  buffId: string
  operatorId: string
  status: 'exact' | 'approximate' | 'display-only'
  dimensions: Array<'direct' | 'same-room' | 'global' | 'faction' | 'class' | 'morale' | 'inventory' | 'order'>
  source: string
  tests: string[]
}
```

### 数据流

```text
Mower JSON/JPG
  -> 无损解析 RosterWorkspace
  -> 主排班结构与语义验证
  -> compileRosterSchedule（保留候选/Free/菲亚梅塔）
  -> EventSimulator + ResourceLedger + RuleRegistry
  -> CalculationReportV2
  -> UI 结果、公式树、时间线和诊断
```

旧 `AppConfig` 路径在迁移期保留作对照；新引擎先只对金标准启用，达到验收阈值后再成为默认。禁止一次性删除旧引擎。

## 四、实施计划

### 阶段 0：冻结基线并修复测试可信度

**文件：**

- 修改：`vite.config.ts`、`package.json`
- 新增：`src/test/goldenScenarios.ts`
- 新增：`src/workbench/compat/fixtures/mower-252-2gold.expected.json`

**TDD 步骤：**

1. 写收集测试/脚本，证明 `.worktrees/**` 被 Vitest 收集。
2. 排除 `.git/**`、`.worktrees/**`、`dist/**`、`.antigravity-bridge/**`。
3. 建立金标准结构，记录输入哈希、Excel 工作表、期望值、容差、数据版本和口径；逐列明确是否含无人机、赤金折算价、收取时点和库存变化。
4. 分离协议金标准与数值金标准；协议必须精确，数值差异固定为显式失败。
5. 增加 `test:core`、`test:golden`、`test:ui`。

**验收：** `pnpm test` 不再输出 `.worktrees/`；连续两次收集数量一致；252 差异成为可重复失败。

### 阶段 1：建立无损主排班编译器

**文件：**

- 新增：`src/scheduler/types.ts`
- 新增：`src/scheduler/compileRosterSchedule.ts`
- 新增：`src/scheduler/compileRosterSchedule.spec.ts`
- 修改：`src/workbench/calculationBridge.ts`
- 保留：`src/workbench/adapter.ts` 作为旧路径

**TDD 步骤：**

1. 用真实 252 写失败测试：但书/龙舌兰仍是有序候选，菲亚梅塔目标保留，宿舍 `Free` 编译为休息池。
2. 实现纯函数 `compileRosterSchedule(workspace, assumptions)`。
3. 建立候选身份和可用性约束，但不在编译阶段删除冲突候选。
4. 把等级推断、导入来源和默认假设写入 `CompiledSchedule.assumptions`。
5. 桥接层增加 `engine: 'legacy' | 'event-v2'`，默认仍用 legacy。

**验收：** 真实 252 的主力、候选顺序、特殊角色、组标签和等级零丢失；输入保持深度相等。

### 阶段 2：通用换班、宿舍与 Free

**文件：**

- 新增：`src/scheduler/rosterRuntime.ts` 及测试
- 新增：`src/simulator/moraleTimeline.ts` 及测试
- 参考并逐步替代：`src/engine/shiftRoster.ts`、`src/engine/morale.ts`

**TDD 步骤：**

1. 第一候选不可用时必须选择第二候选；候选满心情后按策略回岗。
2. 实现 `RosterRuntime.nextCandidate(slot, state)`，严格按 Mower 顺序选择。
3. 建立宿舍床位、宿管、`Free` 池和恢复队列；普通宿管无需替补。
4. 每名干员独立计算工作消耗和宿舍恢复。
5. 心情到 0、到满、跨技能阈值和换岗时生成事件并重算，不用固定小步长。
6. 断言任一时刻一个干员最多位于一处，床位不超容量。

**验收：** 多候选、共享 Free、普通宿管和候选冲突通过；事件单调且无死循环。

### 阶段 3：菲亚梅塔心情交换

**文件：**

- 新增：`src/scheduler/fiammettaPolicy.ts` 及测试
- 修改：`src/simulator/moraleTimeline.ts`、`src/workbench/validate.ts`

**TDD 步骤：**

1. 从真实 252 固定目标顺序，并验证目标可以是当前工作主力。
2. 以 Mower 行为和游戏规则为 oracle，精确定义触发时机、目标选择、心情互换以及排班回填；不得把“互换”擅自简化成单向治疗。
3. 实现 `nextFiammettaSwap(state)`，按候选顺序、阈值、组内相对下限、暖机和休息状态重新筛选，输出目标选择或跳过原因。
4. 交换后立即触发回岗/补位重算，禁止重复占位。
5. 报告显示交换时间、目标、前后心情和受影响设施。

**验收：** 252 时间线按配置处理目标；在预先证明依赖菲亚梅塔的指定样本中，关闭规则后的有效工时与产出差异可追溯。不得把“关闭后必下降”当作所有排班的通用断言。

### 阶段 4：逐订单跑单快照

**文件：**

- 新增：`src/simulator/orderTimeline.ts` 及测试
- 新增：`src/rules/orderRules.ts` 及测试
- 逐步替代：`calculate.ts` 中的 `transformSpecial`

**TDD 步骤：**

1. 1/2 级站只换但书；3 级站换但书+龙舌兰。
2. `OrderAcquisition` 在获取开始固定基础类型与基础工作量；`OrderSnapshot` 在获取完成固定成本、奖励、特殊类型及规则版本。若后续实测证明抽取时点不同，按版本化时点策略替换，不迁就旧字段名。
3. 长期日产采用确定性期望状态转移：对有限订单类型传播概率质量，不使用无种子的蒙特卡洛；调试用单路径回放必须显式固定种子，且不作为正式汇总值。
4. 实现生成前换入、生成时锁定、生成后恢复常驻的三段事件。
5. 换人耗时用 `SimulationAssumptions.swapSeconds` 明示；默认可为 0。
6. 对普通、但书、龙舌兰、组合、U-Official、可露希尔、佩佩分别测试。
7. 报告拆分自然订单、跑单增益和换人损失。

**验收：** 每笔订单只应用一次转换；换回常驻不改变已生成订单。

### 阶段 5：资源账本、仓储与无人机

**文件：**

- 新增：`src/simulator/resourceLedger.ts` 及测试
- 新增：`src/simulator/droneTimeline.ts` 及测试
- 修改：`src/domain/types.ts`

**TDD 步骤：**

1. 赤金不足时订单等待，补入赤金后才能交付。
2. 同一账本记录 EXP、赤金、龙门币、碎片、合成玉、无人机和积压订单。
3. 分别建模制造站仓库容量与贸易站订单上限，并分别应用对应技能；加入满仓/满单停产事件。
4. 收取策略必须是可配置的自动收取假设或用户日程事件，不擅自固定手动收取频率；“及时收取”也必须作为可见假设。
5. 无人机按充能事件累计，容量只限制持有量；溢出和使用分别记账。
6. 无人机减少基础进度，不再次乘效率。
7. 报告同时给出理论、当日可交付、长期可持续三种口径。

**验收：** 每种资源满足“期初 + 流入 - 流出 = 期末”；库存为 0 时无无来源消耗。

### 阶段 6：技能、阵营与联动覆盖

**文件：**

- 新增：`src/rules/registry.ts`、`src/rules/coverage.ts` 及测试
- 拆分：`src/engine/operatorRules.ts`
- 修改：`scripts/audit-riic-data.mjs`
- 新增：`docs/技能规则覆盖报告.md`

**TDD 步骤：**

1. 为每条最高阶段技能生成覆盖条目，不只检查“数据存在”。
2. 显式分类直接效率、同站、全基建、阵营/组织/小队、技能类别、跨设施、心情、库存和订单。
3. 多项术语保存具体内容项；例如莱茵科技、红松骑士团、标准化，以及国家、组织、小队分别列出。
4. 正则解析标记为 `exact` 或 `approximate`；无法量化标记 `display-only`。
5. 增加精英阶段/等级选择，不再默认全员最高阶段。
6. 先让真实 252 涉及技能全部达到 `exact`，再扩展常见组合。

**验收：** 每个 `buffId` 恰有一项；`display-only` 不被隐藏；金标准技能有来源和独立测试。

### 阶段 7：收敛与数值对齐

**文件：**

- 新增：`src/simulator/convergence.ts` 及测试
- 新增：`src/simulator/scenarioRunner.ts` 及测试
- 修改：`src/workbench/calculationBridge.ts`

**TDD 步骤：**

1. 状态签名包含在岗、心情、宿舍队列、订单、库存、无人机和策略游标。
2. 检测到周期时按完整周期求平均；未收敛时报告窗口与残差。
3. 对 252 逐项对齐；先检查事件和守恒，不添加样本专用常数。
4. 输出禁用菲亚梅塔、禁用 Free、禁用跑单、无限赤金四组反事实差异。
5. 增加 252 三赤金、342 纯钱、342 搓玉等回归，防止单样本过拟合。

**验收：** 252 三项核心输出进入预登记容差；模拟时间扩大一倍时长期日产差异小于 0.1%。

### 阶段 8：UI 解释、视觉和导入体验

**文件：**

- 修改：`ValidationPanel.vue`、`WorkbenchShell.vue`、`fileHelpers.ts`
- 新增：`SimulationTimeline.vue`、`FormulaTree.vue`、`AssumptionPanel.vue`
- 新增：`e2e/workbench.visual.spec.ts`

**TDD/验收步骤：**

1. 用户可见位置只显示中文名；内部 ID 仅在技术详情显示。
2. 结果展示口径、假设、规则版本、未量化技能、收敛和资源缺口。
3. 增加事件时间线与公式树，不改变 Mower 主画布布局。
4. 为 JSON/JPG 设置文件、像素和解压大小上限，并给出可恢复错误。
5. 在 1920×1080、1366×768、720×900、150% 缩放下做真实截图回归。
6. 验证键盘导航、焦点、弹窗/画布滚动和主操作可达性。

### 阶段 9：性能、数据更新与发布

**文件：**

- 修改：`vite.config.ts`、`scripts/import-gamedata.mjs`、`scripts/audit-riic-data.mjs`
- 新增：`scripts/benchmark-scenarios.mjs`
- 新增：`docs/数据更新与规则核验.md`、`docs/发布验收清单.md`

**步骤：**

1. 二维码、时间线和规则覆盖报告按功能懒加载。
2. 性能预算：首次交互 <2 秒，静态重算 <100ms，90 天模拟 <2 秒，真实 JPG 导入 <5 秒。
3. 移除个人绝对路径，GameData 改用显式参数并提供缺参示例。
4. 数据导入生成版本清单、技能覆盖差异和规则回归列表。
5. 发布前执行类型检查、核心/金标准/视觉测试、数据审计、构建与隐私扫描。

**验收：** 新机器可复现数据和测试；包体/性能有阈值；发布不依赖个人目录。

### 阶段 10：非核心兼容项与优化

仅在阶段 0–9 完成后评估：

- 执行 `backup_plans`、触发器 AST 和 `roomTask`；此前只保证无损往返。
- 自动排班、布局比较、无人机最优分配和目标函数优化。
- 会客室、办公室、训练室和加工站的非货币效果。
- Windows 桌面封装、安装包、自动更新和跨设备同步。

## 五、测试矩阵

| 层级 | 必测内容 | 失败含义 |
| --- | --- | --- |
| 协议 | JSON/JPG 往返、未知字段、副表保存、等级推断 | 兼容回归，禁止发布 |
| 编译 | 候选顺序、Free、菲亚梅塔、跑单角色、中文名 | 核心语义丢失 |
| 规则 | 单技能、同站、跨设施、阵营、类别、订单 | 局部公式错误 |
| 事件 | 心情、换班、恢复、订单、收取、无人机 | 时间线错误 |
| 守恒 | 干员唯一、资源账本、容量、事件单调 | 引擎结构错误 |
| 金标准 | 252 二赤金及扩展场景 | 综合算法未对齐 |
| UI | 三档视口、150% 缩放、键盘、错误恢复 | 用户不可用 |
| 性能 | 大图、长周期、全规则、包体 | 低配置风险 |

## 六、里程碑与退出条件

### M1 可信基线

- 测试不扫描 `.worktrees`。
- 252 差异被固定为可重复金标准。
- 编译不再丢失核心主排班语义。

### M2 动态排班闭环

- 多候选、Free、普通宿管和菲亚梅塔进入同一时间线。
- 干员唯一占位和宿舍容量始终满足。

### M3 跑单与资源闭环

- 逐订单快照正确。
- 赤金、订单、无人机和仓储满足守恒。
- 报告区分理论、当日可交付和长期可持续。

### M4 数值对齐

- 252 三项核心输出达到登记容差。
- 扩大模拟窗口后长期日产变化 <0.1%。
- 真实样本涉及技能全部为 `exact`。

### M5 可交付

- Mower 主画布通过真实浏览器视觉验收。
- 导入错误可恢复，规则与假设可解释。
- 完整验证链可在新机器复现。

## 七、实施顺序与投入建议

1. 阶段 0–1：1–2 个开发日，建立可信入口。
2. 阶段 2–3：4–7 个开发日，完成动态排班和菲亚梅塔。
3. 阶段 4–5：4–7 个开发日，完成跑单和资源闭环。
4. 阶段 6：首轮 3–5 个开发日，之后随版本持续维护。
5. 阶段 7：2–4 个开发日；仍有差异时按反事实报告定位，不并行堆 UI。
6. 阶段 8–9：3–5 个开发日。
7. 各阶段由 Gemini/Antigravity 承担主要检索和实现；GPT 只审查紧凑 diff、运行最终验证和处理跨模块回归。

## 八、下一步

从阶段 0 开始：先排除 `.worktrees` 测试污染并建立 252 数值金标准；随后实现 `compileRosterSchedule`。这两项不改变现有产出算法，却能让后续动态机制建立在可信、可比较的基线上。
