# 明日方舟基建平均每日产出收益预测软件计划书

## 1. 项目定位

开发一款本地优先的明日方舟基建收益预测软件。用户配置基建设施、等级、生产策略、干员与轮班/跑单方案后，软件给出可复现、可解释的平均每日产出，并明确显示电力、原料、心情、仓储和订单容量等约束。

核心输出固定为：

- 作战记录：每日张数与总 EXP 价值。
- 赤金：每日制造数量与按 500 龙门币/条折算的价值。
- 贵金属订单：每日订单数量、赤金消耗、龙门币收入。
- 源石碎片：每日制造数量，以及可选的原料与龙门币成本。
- 合成玉：每日源石订单数量、碎片消耗、合成玉收入。
- 无人机：每日理论充能数、实际可用数、溢出数、分配去向与增量收益。
- 心情：每名干员消耗/恢复速度、预计耗尽时间、轮班覆盖率。
- 电力：总发电、总耗电、余量或缺口。

首要原则是“同一配置永远得到同一结果”。默认报告应使用数学期望，不使用无种子的随机订单模拟。

## 2. 调研结论与数据源策略

### 2.1 已核对的数据源

1. [PRTS 罗德岛基建](https://prts.wiki/w/罗德岛基建)及制造站、贸易站、发电站、控制中枢子页面：用于核对机制、特殊订单优先级、暖机和页面尚未结构化的规则。
2. [明日方舟工具箱的基建技能筛选页](https://arkntools.app/#/riic)：适合作为干员搜索、设施分类和技能效果展示的交互参考，不应被当作计算 API。
3. `C:\Users\Panda-Panta\Downloads\Compressed\ArknightsGameData-master\zh_CN\gamedata\excel\building_data.json`：主要机器可读数据源，包含房间上限/等级、电力、进驻人数、制造配方、干员基建技能和技能描述。
4. 同目录的 `character_table.json`、`item_table.json`、`data_version.txt`：用于干员和物品名称、稀有度、版本标记。当前本地快照为 `v76 / 76.0.0`，变更日期为 2026-07-30。
5. `C:\Users\Panda-Panta\Downloads\Compressed\arknights-mower-main\base_calculator`：可参考其领域对象拆分和测试样例，但不直接复用计算结果。

### 2.2 数据优先级

按以下顺序解决冲突：

1. 用户确认的项目规则覆盖项；
2. 当前版本 GameData 中可结构化读取的值；
3. PRTS 中经过核对的机制说明；
4. 软件内版本化的人工规则表；
5. mower 代码只作为线索，不作为事实来源。

每份报告都记录 `game_data_version`、规则包版本和生成时间。数据过旧时只提示，不阻止离线计算。

### 2.3 当前发现的冲突与处理决定

- 用户资料写“每个站上限为 5”，但 GameData 与 PRTS 均显示制造站最多 5、贸易站最多 5、发电站最多 3。软件采用数据驱动上限，并将“制造+贸易+发电=9”作为布局校验。
- 当前网页资料新增了“千金的眼光”，且 α/β 高品质技能分别约需连续工作 3/5 小时达到给定峰值；用户给出的峰值概率保留，但必须加入暖机状态。多个品质技能叠加模型目前仍不完全确定，报告需标记“观测规则/不确定”。
- GameData 显示源石碎片配方还消耗源岩/装置与龙门币。默认总览按用户要求展示毛产出，另提供“计入原料成本”的净收益视图。
- mower 原型中的 `DataLoader` 依赖当前目录不存在的 `building_data_full.json` 与带哈希文件名；需要改为直接读取用户给出的 GameData 路径。
- mower 原型用蒙特卡洛生成一天订单，会使相同配置产生不同结果；新引擎改用确定性期望。
- mower 对但书/龙舌兰使用的技能 ID 与当前 GameData ID 不一致，并错误地让 U-Official 保留原订单的 3/4 赤金需求；新规则以 `buffId` 映射和显式特殊订单转换器实现。
- mower 的无人机速率会给空发电站也加 5%，心情消耗又被强行限制为最低 0.1；这两点均不沿用。心情允许为 0 或负数。

## 3. 产品范围

### 3.1 第一版必须具备

- 创建、保存、复制、导入和导出多个基建方案。
- 编辑 9 个产出设施的类型与等级；分别配置制造产品和贸易策略。
- 编辑控制中枢、4 个宿舍、会客室、加工站、办公室、训练室的等级与进驻干员。
- 按干员名、拼音、设施、技能效果筛选干员；显示技能解锁精英阶段/等级。
- 校验同一干员不能同时出现在多个工位，跑单临时角色除外。
- 支持常驻阵容、替班阵容和跑单阵容。
- 计算稳态平均每日收益及一个指定 24 小时日程的收益。
- 电力不足时保留全部编辑能力，但隐藏/禁用收益数值，显示具体缺口与可行修改建议。
- 每个结果均可展开查看基础值、1% 进驻加成、技能加成、全局加成、有效时间和公式。
- 导出 JSON 配置与 Markdown/CSV 报告。

### 3.2 第二阶段

- 自动排班：在干员心情、宿舍恢复和换班次数约束下最大化 EXP、龙门币或合成玉。
- 自动布局比较：例如 243、252、153 等方案的可持续收益对比。
- 无人机最佳分配：赤金、经验、贸易站之间按目标函数自动分配。
- 库存流模拟：初始赤金/碎片库存、收取间隔、订单上限和制造仓库容量。
- 从新版 GameData 目录一键刷新，生成规则差异报告。

### 3.3 暂不纳入收益总额

会客室线索、办公室招募刷新、训练室专精和加工站副产品不折算成统一货币。它们的进驻与技能必须进入全局联动计算，但单独展示“非货币效果”。不要把 EXP、龙门币、合成玉强行合并为一个未经用户定义的总价值。

## 4. 领域模型

建议采用以下核心对象，配置文件带 `schemaVersion`：

| 对象 | 关键字段 |
| --- | --- |
| `BasePlan` | 名称、数据版本、目标、计算模式、9 个产出设施、功能设施、无人机策略、日程 |
| `Room` | 唯一 ID、设施类型、等级、工位、产品/策略、仓储/订单上限 |
| `OperatorState` | `charId`、精英阶段、等级、初始心情、所在工位、进驻开始时间 |
| `Shift` | 开始/结束时间、房间、常驻队伍、跑单队伍、收取动作、无人机动作 |
| `BuffDefinition` | `buffId`、适用房间、解锁条件、效果节点、叠加组、优先级、暖机函数 |
| `OrderTemplate` | 赤金/碎片需求、报酬、基础时长、概率、是否受效率/无人机影响 |
| `CalculationContext` | 房间数量、等级、阵营人数、技能图标数、全局资源变量、时间点 |
| `DailyReport` | 分设施产出、资源流、心情、电力、警告、计算解释、版本信息 |

技能不要只靠中文描述正则解析。应采用“通用效果节点 + 少量显式处理器”的规则系统：

- 通用节点：固定加成、按设施数量、按同房干员、按阵营、按订单上限、取最高、清零、心情修正。
- 状态节点：暖机、工作时长、心情阈值、特定干员/设施存在、全局资源生成与消费。
- 特殊处理器：佩佩、可露希尔、U-Official、但书、龙舌兰以及后续无法用通用节点表达的技能。
- 每个节点保存来源、置信度和测试编号，未知技能在界面中明确显示“尚未量化”，不能静默忽略。

## 5. 计算模型

### 5.1 电力与布局校验

```text
产出设施数量 = 制造站数 + 贸易站数 + 发电站数 = 9
总发电 = Σ 发电站各等级提供电力
总耗电 = Σ 所有耗电设施的等级耗电
可计算 = 总发电 >= 总耗电
```

三级发电站为 270；其他设施耗电由 `building_data.rooms[*].phases[*].electricity` 读取，避免把用户给出的表永久硬编码。电力不足时返回完整的验证对象，但 `incomeReport=null`。

### 5.2 制造站

对每个时间片：

```text
有效效率倍率 E = 1 + 进驻人数×0.01 + 房间技能加成 + 全局技能加成
产物数量 = 有效工作分钟 × E / 单件基础分钟
```

中级作战记录基础 180 分钟、1000 EXP；赤金 72 分钟、价值 500 龙门币；源石碎片 60 分钟。配方基础时间优先从 GameData 的 `costPoint` 换算，用户规则作为回归断言。

制造结果同时显示“物理数量”和“折算价值”。源石碎片的输入材料、龙门币成本独立列出，不能从毛产出中悄悄扣除。

### 5.3 贸易站普通订单

普通贵金属订单的分布按贸易站等级选择；三级站常规为 30%/50%/20%，峰值 α 为 15%/30%/55%，峰值 β 为 5%/10%/85%。

稳态平均使用更新奖励过程，而不是“先随机出一天订单”：

```text
平均基础时长 T = Σ(pᵢ × tᵢ)
平均报酬 R = Σ(pᵢ × rewardᵢ)
平均原料 C = Σ(pᵢ × costᵢ)
自然订单/日 = 1440 × E / T
龙门币/日 = 自然订单/日 × R
赤金消耗/日 = 自然订单/日 × C
```

注意不能使用 `Σ(pᵢ × rewardᵢ/tᵢ)` 代替 `R/T`。24 小时有限时域模式可用按秒/小时间隔的动态规划计算“截至日末已完成订单”的期望，并把未完成订单进度带到下一天。

源石订单仅允许三级贸易站：2 碎片、20 合成玉、120 分钟。自然获取时间受贸易效率；是否接受无人机加速作为规则字段，不散落在 UI 代码中。

### 5.4 高品质与特殊订单

品质技能的概率是时间函数：刚进驻、暖机中、峰值三个阶段分别计算。换工位或撤出后按资料规则重置暖机计时。多个技能同房时使用版本化叠加表；没有可靠模型时只能提供“按 β 覆盖”“采用观测分布”两个明确选项，不得自行相加。

特殊订单按以下优先级只应用一种转换：

1. 佩佩：0 赤金、1000 龙门币、固定 270 分钟，不受订单效率影响。
2. 可露希尔：2 赤金、1200 龙门币、基础 144 分钟。
3. U-Official：固定 2 赤金、1000 龙门币，时长继承原订单。
4. 但书：α 增加 1 赤金/500 龙门币；β 增加 2 赤金/1000 龙门币。
5. 龙舌兰：α 增加 250 龙门币；β 增加 500 龙门币。

每个转换器需要分别声明：触发谓词、目标订单、优先级、修改字段、是否受效率、是否受无人机、跑单时是否锁定。

### 5.5 跑单

跑单不是简单把临时干员加入效率队伍。订单在生成瞬间锁定类型和特殊属性，因此模型将一次订单拆为：

1. 生成前事件：换入但书/龙舌兰/可露希尔等；
2. 生成事件：依据当时概率与特殊订单优先级锁定订单；
3. 生成后事件：撤出跑单干员，恢复常驻队伍；
4. 后续订单倒计时使用正确的效率队伍和订单固定属性。

默认预设提供“不跑单”“但书”“但书+龙舌兰”。中途换人造成的短暂效率差可按用户要求默认忽略，但报告必须写明该假设。

### 5.6 无人机

```text
总充能倍率 = 1 + Σ(每名实际进驻发电站干员的 5%) + Σ技能加成
理论无人机/日 = 1440 / 6 × 总充能倍率
无人机加速分钟 = 实际使用无人机数 × 3
```

基础 100% 属于全基建，不能为每个发电站重复计算；空发电站不提供 5%。持有上限不停止充能，因此报告区分理论充能、实际使用和溢出。若用户未配置收取/使用时刻，默认“全部及时使用”，并标记为理论上限。

无人机减少基础耗时，增量产出按目标配方或订单的基础进度换算，不能再次乘生产效率。佩佩等固定订单是否可加速由订单规则明确控制。

### 5.7 心情和排班

每名干员分别计算，不能只给房间一个平均值：

```text
个人心情变化率 = -1
                 + 制造/贸易人数减免(0/0.05/0.1)
                 + 控制中枢基础全员减免
                 + 房间技能、个人技能和全局技能修正
```

正值表示恢复、负值表示消耗（实现中统一一个符号约定）。控制中枢自身的基础减免与对其他工作区的全员减免要分开建模。心情到 0 后，干员进入注意力涣散，其基础 1% 与大部分技能失效，事件引擎立即重算房间效率。心情可以保持不变或在工作设施中恢复，不设置人为最低消耗。

第一版支持两种模式：

- 稳态模式：用户直接给出每队每日有效工作时长/覆盖率，适合快速比较。
- 日程模式：配置 24/48 小时换班与宿舍恢复，按事件精确推进，适合验证可持续性。

### 5.8 资源可持续性

收入页同时给出：

- 理论订单收入：假定赤金/碎片库存无限。
- 当日可交付收入：计入初始库存和当日产出。
- 长期可持续收入：以制造供给约束贸易消耗。

如果赤金或碎片不足，不把未交付订单算作实际收入；显示缺口、积压订单和达到稳定库存所需调整。仓库/订单上限与收取间隔也进入日程模式，避免把满仓停工当成持续生产。

## 6. 界面与交互

建议主界面为五栏/五步工作流：

1. **布局**：九宫格编辑产出设施；顶部持续显示 `制造/贸易/发电 = x/y/z` 和电力条。
2. **设施与干员**：点击房间进入工位；右侧抽屉按 arkntools 风格筛选干员，技能文本旁显示“已量化/部分量化/仅展示”。
3. **排班与跑单**：时间轴编辑常驻、替班、暖机、跑单、收取与无人机动作。
4. **每日收益**：卡片展示 EXP、赤金价值、订单龙门币、碎片、合成玉；毛产出和可持续产出可切换。
5. **计算明细**：按设施展开公式、技能叠加顺序、警告、来源与数据版本。

关键交互要求：

- 电力不足时收益区显示“无法计算：缺少 N 电力”，其余页面不锁定。
- 冲突干员、超出工位、非法源石策略、设施数量不为 9 时就地提示。
- 更改任一配置后增量重算，目标为桌面设备上 100 毫秒内更新静态方案。
- 提供 243、252 等常用布局模板，但模板只是起点，不替用户决定生产目标。
- 数值保留原始精度计算；界面默认数量 2 位小数、价值取整数，并支持查看未舍入值。

## 7. 技术方案

推荐使用 Vue 3 + TypeScript + Vite 开发计算与界面，并以 Tauri 2 打包 Windows 桌面程序。理由是该产品无需服务器，数据量小，TypeScript 便于让计算规则、UI 类型与测试共用；Tauri 能提供本地文件选择、配置持久化和较小安装包。

建议目录：

```text
src/
  domain/          纯领域类型与约束
  data/            GameData 导入、规范化、版本差异
  rules/           技能 DSL、特殊处理器、来源元数据
  engine/          电力、制造、贸易、无人机、心情、资源流
  scheduler/       时间线与事件推进
  optimizer/       第二阶段优化器
  stores/          方案状态与持久化
  views/           布局、排班、收益、明细
  components/      房间卡、干员选择器、公式树、警告
tests/
  fixtures/        固定 GameData 小样本
  unit/            公式和单技能测试
  scenario/        完整基建方案金标准
scripts/
  import-gamedata  生成规范化 JSON 与差异报告
rules-data/
  overrides/       人工核对规则，按版本保存
```

计算引擎必须是无 UI 依赖的纯函数库，以便命令行批量验证和将来迁移到其他前端。

## 8. 数据导入流程

1. 用户选择 GameData 根目录，程序精确验证 `zh_CN/gamedata/excel/building_data.json` 等文件。
2. 读取版本、房间、配方、干员、技能和物品表。
3. 将原始枚举与数值规范化为内部 schema，保留原始 `charId`、`buffId` 和来源路径。
4. 对技能描述只做辅助提取；与已知规则不一致时生成待审核差异，不自动覆盖显式规则。
5. 执行完整性检查：未知房间类型、丢失技能、重复 ID、非法百分比、未覆盖特殊订单。
6. 生成 `normalized-data.json`、`rule-coverage.json`、`data-diff.md`。
7. 应用程序只读取规范化数据和规则包，避免运行时直接依赖巨大原始文件。

## 9. 测试与验收

### 9.1 单元测试

- 0/1/2/3 人制造与贸易基础效率。
- 各等级房间电力、工位、仓储和订单上限。
- 三类制造配方的 100%、任意技能效率和跨日小数产量。
- 三级贸易站常规、α、β 分布的概率和为 1，期望赤金分别为 2.9、3.4、3.8。
- 五种特殊订单的字段转换与优先级，两两组合不得叠加成第六种订单。
- U-Official 始终为 2 赤金/1000 龙门币，时长继承原订单。
- 佩佩固定 270 分钟且不受订单效率影响。
- 空发电站不产生 5% 进驻加成；多发电站只共享一份 100% 基础充能。
- 心情速率可为正、零、负；到 0 的同一时刻失效并重算。
- 电力不足时报告无收益但配置不被修改。

### 9.2 场景测试

- 纯 100% 赤金制造站：一天 20 条、价值 10000 龙门币。
- 纯 100% 中级作战记录制造站：一天 8 张、8000 EXP。
- 纯 100% 源石碎片制造站：一天 24 个碎片。
- 三级源石贸易站 100%：一天 12 单、消耗 24 碎片、获得 240 合成玉。
- 赤金制造不足、恰好平衡、过量三种资源流状态。
- 243 与 252 布局在同一干员池、同一无人机策略下的对比。
- 但书、但书+龙舌兰跑单，验证换人只影响订单转换，不污染常驻效率。
- 高品质技能在 0h、3h、5h、撤出重进四个节点的分布变化。

### 9.3 交叉验证

- 用 PRTS 基础表逐项核对固定规则。
- 用 arkntools 页面抽样核对干员名、解锁条件与技能描述。
- 用 GameData 对全体干员做覆盖率扫描，任何新增/未知技能导致 CI 警告。
- mower 只用于比较已知样例；若结果不同，以公式树定位差异，不能为“对齐旧结果”而修改正确公式。
- 至少选 10 份真实游戏副手简报，比较连续 3 日实际产出；基础方案误差目标小于 1%，含手动收取/换班方案解释全部剩余差异。

## 10. 里程碑

| 阶段 | 工作内容 | 退出条件 |
| --- | --- | --- |
| M0 规则冻结 | 建立来源表、冲突清单、内部 schema、金标准样例 | 所有基础规则有来源和测试编号 |
| M1 数据层 | GameData 导入、规范化、版本与差异报告 | 425 名本地干员可索引，未知规则被列出而非丢弃 |
| M2 静态引擎 | 电力、制造、贸易、特殊订单、无人机、毛/净产出 | 固定配置完全确定，基础验收全部通过 |
| M3 配置 UI | 布局、等级、干员筛选、方案保存、实时校验 | 电力不足仍可编辑，合法方案可生成日报 |
| M4 时间引擎 | 心情、暖机、轮班、跑单、收取、库存 | 24/48 小时事件可回放，每个变化点可解释 |
| M5 验证发布 | 实际简报对比、性能、安装包、使用说明 | 真实样本达误差目标，发布包可离线运行 |
| M6 优化器 | 自动排班、布局比较、无人机最优分配 | 可选择目标并返回可执行方案及收益差异 |

建议按 M0→M5 先完成可信计算器，再开发优化器。优化器依赖规则覆盖度，过早加入会把基础误差放大成错误建议。

## 11. 风险与应对

- **数据版本漂移**：本地 GameData 比当前网页旧。所有缓存带版本，更新时生成差异报告，新增技能默认“仅展示”而非猜测。
- **复杂技能难以通用化**：采用效果 DSL 与显式处理器并存，规则覆盖率成为发布门槛。
- **高品质叠加未完全研究**：显示置信度和可选模型，不把观测分布宣传为官方公式。
- **实际产出受操作习惯影响**：同时提供理论、24 小时日程和长期可持续三种结果，并把收取、溢出、库存假设列在报告顶部。
- **统一价值误导**：EXP、龙门币、合成玉分栏；只有用户设置兑换权重后才展示自定义综合分。
- **旧代码带入错误**：mower 领域模型可借鉴，但订单、无人机、心情和数据加载必须用金标准测试重写。

## 12. 第一版完成定义

第一版只有同时满足以下条件才算完成：

1. 合法布局能稳定输出五类用户要求的收益，重复计算完全一致。
2. 电力不足时明确阻止收益计算，但任何设施和干员仍可继续编辑。
3. 普通/品质/特殊/跑单订单均有可展开的计算说明。
4. 心情耗尽、暖机重置、换班和全局技能会在正确时间点改变效率。
5. 赤金与碎片短缺不会被计算成已交付收入。
6. 无人机基础 100% 只计算一次，空站没有进驻加成，溢出可见。
7. 所有未量化技能均在报告中列出，不存在静默忽略。
8. 安装包、数据更新说明、配置导入导出和完整测试报告齐备。

