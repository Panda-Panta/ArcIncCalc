# 一键智能排班 — 实施计划

## 先回答你的具体疑问

### L51 到底在检查什么

用大白话说：**如果你的 mower JSON 文件里有任何"人工配置过的东西"，程序就完全拒绝自动生成**。具体来说，以下任何一条为真就拒绝：

- `conf` 里的 `workaholic`、`resting_priority`、`exhaust_require` 等列表不是空数组（你配了策略）
- 有 `backupPlans`（你配了备用计划）
- 任何干员槽位有 `replacements`（你配了候补）
- 任何干员槽位有 `groupId`（你配了换班分组）
- 有任何未识别的额外字段

**所以实际上，只要你的工作台不是一个从零创建的空白模板，`generateAutomaticRoster` 就不会工作。**

### "换班死锁"是什么

举个极端例子：A 组 3 人在工作、B 组 3 人在宿舍恢复。A 组心情耗尽要换班，但宿舍只有 3 张 Free 床位（全被 B 组占着）。B 组还没恢复完，不能腾位。A 组也没法下班。结果：A 组疲劳占岗，B 组继续睡，所有人卡住。

但你说的对——实际中 mower 的策略是"让被换下来的人等着"，现有代码（`SHIFT_DEFERRED_RECOVERED`）也确实是这么处理的：暂时保留原班，等有床位了再换。所以不是真死锁，只是换班延迟。**新方案不需要特殊处理这个问题。**

### `generateAutomaticRoster` 是什么

就是你在 `WorkbenchShell.vue` 里点击"一键生成排班"按钮时调用的那个算法。它在 [automaticRoster.ts](file:///d:/Project/Arknights/ArcIncCalc/src/optimizer/automaticRoster.ts) 里。

`WorkbenchShell.vue` 的 `handleAutoGenerate` 函数（[L320-388](file:///d:/Project/Arknights/ArcIncCalc/src/components/workbench/WorkbenchShell.vue#L320-L388)）做的事是：
1. 从 localStorage 读干员库
2. **把当前工作台清空**（L351-357：把所有槽位设为 empty）
3. 调用 `generateAutomaticRoster(cleanWorkspace, ...)`
4. 结果成功就 `store.loadWorkspace(newWorkspace)` 导入到编辑器

新方案要替换的就是这个流程中的第 2-3 步。

### "该程序不支持有起始点的计算吗"

现有的 `generateAutomaticRoster` **确实不支持**——它的前置条件要求所有生产设施槽位为空（L54），并且不能有任何策略配置（L51）。

新方案会支持：保留用户已手动放置的干员，只填充空位。

---

## Proposed Changes

### 核心算法模块

---

#### [NEW] [smartRoster.ts](file:///d:/Project/Arknights/ArcIncCalc/src/optimizer/smartRoster.ts)

新的一键排班核心引擎，替代 `generateAutomaticRoster`。

**导出接口**：

```typescript
export interface SmartRosterOptions {
  seed?: number              // 随机种子，默认 42
  trials?: number            // 静态构建轮次，默认 5
  maxStaticEvals?: number    // 第一层静态评估预算，默认 3000
  simulationTopK?: number    // 进入仿真验证的骨架数，默认 8
  enableDeepSearch?: boolean // 是否启用第三层邻域搜索，默认 true
}

export interface SmartRosterProgress {
  phase: 'building' | 'simulating' | 'searching' | 'done'
  phaseProgress: number      // 0-1
  currentTrial?: number
  totalTrials?: number
  bestScore?: number
  label: string
}

export interface SmartRosterResult {
  status: 'draft' | 'blocked'
  workspace: RosterWorkspace | null
  diagnostics: { code: string; message: string }[]
  phases: {
    static: { trials: StaticTrial[]; bestScore: number | null }
    simulation: { candidates: SimulatedCandidate[]; bestId: string | null } | null
    search: IncomeSearchResult | null
  }
}

export function runSmartRoster(
  base: RosterWorkspace,
  entries: readonly OwnedOperatorInput[],
  options?: SmartRosterOptions,
  onProgress?: (p: SmartRosterProgress) => void,
): SmartRosterResult
```

**第一层：静态贪心构建**（`buildGreedyRoster` 内部函数）

1. 扫描工作台，识别**用户已放置的干员**（`occupant.kind === 'operator'`），标记为锁定
2. 识别**空位**（`occupant.kind === 'empty'` 且无 replacements/groupId）
3. 加载组合候选：`admitCombinationCandidates` + `CROSS_ROOM_TEMPLATES`
4. 计算空布局基线 82 分：`projectRosterOutput(workspace)`
5. 对每个候选组合/跨房间模板，尝试放入空位，计算**全表 82 分增量**（`projectRosterOutput(放入后) - 基线`）
6. 按增量降序，贪心放入最优组合
7. 每放入一个组合后更新基线，重新评估剩余候选
8. 空位用散件填充（复用现有 L128-149 的逻辑，但去掉空布局限制）
9. 中枢 5 人也参与竞争
10. `assignBackups` 分配候补
11. 每个 trial 使用不同随机种子打乱枚举顺序
12. 输出 Top-8 个骨架

**第二层：仿真验证**（`validateWithSimulation` 内部函数）

1. 对 Top-8 骨架，编译成 `CompiledSchedule`（`compileRosterSchedule`）
2. 调用 `simulateSchedule`，参数：`warmupHours: 24, sampleHours: 72`
3. 用两个种子 × 两档步长 = 4 组对照
4. 用 `summarizeIncome` 检查完整性
5. 按 `scoreProduction` 的 82 分排序
6. 保留 Top-3

**第三层：邻域深度搜索**（复用 `rosterIncomeSearch`）

1. 取仿真 Top-1 作为 baseline
2. 调用 `runRosterIncomeSearch`，参数：
   - `mode: 'hill-climb'`
   - `maxDepth: 3`
   - `maxCandidates: 12`
   - `includeControlMains: true`
   - `includeProductionMains: true`
3. 输出最终推荐

---

#### [NEW] [smartRosterWorker.ts](file:///d:/Project/Arknights/ArcIncCalc/src/optimizer/smartRosterWorker.ts)

Web Worker 入口，模式与现有 `automaticRosterWorker.ts` 一致：

```typescript
import { runSmartRoster } from './smartRoster'

self.onmessage = (event) => {
  try {
    const { base, entries, options } = event.data
    const result = runSmartRoster(base, entries, options, (progress) => {
      self.postMessage({ type: 'progress', progress })
    })
    self.postMessage({ type: 'complete', result })
  } catch (error) {
    self.postMessage({ type: 'error', error: String(error) })
  }
}
```

---

### 前端修改

---

#### [MODIFY] [WorkbenchShell.vue](file:///d:/Project/Arknights/ArcIncCalc/src/components/workbench/WorkbenchShell.vue)

修改 `handleAutoGenerate` 函数（L320-388），将算法从 `generateAutomaticRoster` 替换为 `smartRosterWorker`：

**改动要点**：
1. 不再清空工作台（删除 L351-357 的清空逻辑）——保留用户已放置的干员
2. 使用 `smartRosterWorker` 在 Web Worker 中异步执行
3. 添加进度显示（phase + percentage）
4. 完成后 `store.loadWorkspace(result.workspace)` 自动导入到编辑器
5. 自动触发 `handleCalculate()` 重新计算收益

```typescript
// 新的 handleAutoGenerate：
async function handleAutoGenerate(): void {
  // ... 读取干员库 ...
  isGeneratingRoster.value = true
  const worker = new Worker(
    new URL('../../optimizer/smartRosterWorker.ts', import.meta.url),
    { type: 'module' }
  )
  worker.postMessage({
    base: JSON.parse(JSON.stringify(toRaw(store.workspace))),  // 不再清空！
    entries: inventoryEntries,
    options: { seed: simSettings.value.seed, trials: 5, maxStaticEvals: 3000 }
  })
  worker.onmessage = (event) => {
    if (event.data.type === 'progress') {
      // 更新进度UI
    } else if (event.data.type === 'complete') {
      const result = event.data.result
      if (result.status === 'draft' && result.workspace) {
        store.loadWorkspace(result.workspace)  // 自动导入到编辑器
        handleCalculate()                       // 重新计算收益
      }
    }
    // ...
  }
}
```

#### [MODIFY] [RosterDraftPanel.vue](file:///d:/Project/Arknights/ArcIncCalc/src/components/workbench/RosterDraftPanel.vue)

修改自动生成模式的逻辑，使用 `smartRosterWorker` 替代 `automaticRosterWorker`：

**改动要点**：
1. Worker 改为 `smartRosterWorker.ts`
2. 添加进度条 UI（显示 building → simulating → searching → done）
3. 显示仿真结果对比表（各骨架的 82 分、仿真 82 分）
4. 添加 `apply` 事件，用于将结果自动导入到上层编辑器

#### [MODIFY] [ScheduleSimulationPanel.vue](file:///d:/Project/Arknights/ArcIncCalc/src/components/workbench/ScheduleSimulationPanel.vue)

L62：添加 `@apply` 事件处理，当 `RosterDraftPanel` 发出 apply 事件时，将生成的 workspace emit 到上层：

```vue
<RosterDraftPanel
  :workspace="workspace"
  :inventory="inventory"
  @invalidate="clear"
  @draft-change="draftForSearch=$event"
  @simulate="draft=>run(draft,'组合草案')"
  @apply="workspace => $emit('apply', workspace)"  <!-- 新增 -->
/>
```

---

## 不需要修改的现有模块

| 模块 | 用途 |
|------|------|
| `projectControlOutput` / `projectRosterOutput` | 第一层静态评估 |
| `simulateSchedule` + `compileRosterSchedule` | 第二层仿真 |
| `rosterIncomeSearch` / `multiStartSearch` | 第三层深度搜索 |
| `combinationCandidates` + `crossRoomTemplates` | 组合库加载 |
| `assignBackups` | 候补分配（二分图匹配）|
| `summarizeIncome` + `compareIncome` | 仿真结果对照 |
| `scoreProduction` | 82 评分公式 |

---

## Verification Plan

### Automated Tests

#### `smartRoster.spec.ts`

1. **243 标准布局空白起点**：从空工作台生成，验证所有槽位填满、无重复干员、候补齐全
2. **243 布局有起始点**：预先放置 3 个干员到指定位置，验证这 3 个干员在结果中保留在原位
3. **252 布局**：制造 5 + 贸易 2 + 发电 2，验证能正确生成
4. **仿真层能过滤掉静态高分但实际差的方案**：构造一个心情 2 小时耗尽的组合，验证仿真后被淘汰
5. **跨房间组合参与竞争**：验证在合适条件下跨房间模板能被选用
6. **预算控制**：3000 次评估内完成
7. **可重复性**：相同种子和输入，输出完全一致

```bash
pnpm vitest run src/optimizer/smartRoster.spec.ts
```

### Manual Verification

- 在界面录入标准干员 Box，点击一键生成
- 确认结果自动出现在排班表编辑器中（不需要手动导入）
- 确认生成过程中有进度提示（building → simulating → done）
- 点击"计算收益"确认 82 分合理
- 在桌面客户端 `D:\Tools\ArcIncCalc\ArcIncCalc.exe` 中验证

---

## 已确认的设计决策

1. **特殊心情干员**：先仿真获得实际工休数据，然后用仿真数据（而非静态 77.5% 估算）计算产出。仿真结果中的特殊干员工休比直接作为最终数据。
2. **进度展示**：放在"自动生成排班"按钮的下方一整行。
3. **有起始点的模拟**：支持保留用户已放置的干员，只填充空位。
