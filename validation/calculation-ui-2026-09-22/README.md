# 计算配置与主班头像验收

- 计算产出、自动排班的无人机选项均从当前 B101–B303 制造站/贸易站生成；发电站不作为候选。
- `droneRoomId` 指定实际加速设施；制造加速与下一无人机事件均按该房间过滤。旧的产品类别与 `droneTradingRoomId` 调用继续兼容。
- 无人机目标失效时不偷偷改投其他房间；弹窗回退“不使用无人机加速”。
- 计算弹窗默认使用当前干员库，可取消并以最高练度技能快照计算，不改动保存的干员库。
- 自动排班的动态评分、深度微调和生成后计算传递同一房间目标。
- 设置页移除无人机目标控件；主班显示头像与姓名，头像失败时保留姓名。

## 页面检查

在本地浏览器实际操作并查看截图：

1. 计算弹窗显示 B101、B102、B201、B202、B301、B302（默认布局其余三间为发电站）。
2. 可切换到 B102 并取消使用干员库；勾选框、文字及按钮布局正常。
3. 主班选择迷迭香后，左侧按钮显示实际头像和姓名。
4. 设置页不存在余量无人机或贸易站加速控件。
5. 临时录入干员后打开智能排班弹窗，确认同样显示具体房间目标；未启动自动排班。
6. 检查完成后撤销临时干员库与主班改动。

## 自动验证

- 界面、计算流程与 WorkbenchShell：41 项通过。
- 制造站/贸易站定向加速、无效目标及副表回归：19 项通过。
- 类型检查与生产构建通过。
- 初次全量并发：1071 项通过，14 项超时、1 项干员库刷新回归失败，另有 4 次测试进程状态通信超时。干员库刷新问题已修复且 WorkbenchShell 全部复测通过；长耗时测试另行单进程复测，见对应日志。

## 长耗时复测结果

- 单进程复测原超时的自动排班/分支/组合/搜索用例：7 项通过。
- 另有 6 项跑单用例仍超过原默认 5 秒，使用命令行 `--testTimeout=30000` 复测：6 项全部通过；不修改断言或仓库测试时限。
- 原全量运行发现的 15 项失败均已修复或通过独立复测。原全量命令本身仍为失败记录，不能标为一次性全量通过；4 次进程状态通信超时未在独立复测中重现。
- 最终计算弹窗用例 2 项通过，`git diff --check` 通过。
