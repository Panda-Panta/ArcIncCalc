# 实际练度散件排班修复验收

- 分支：mode/shift-run；修改前源文件备份：source-before.zip。
- 原问题：用户练度表导入 376 人，在默认 243 布局生成 0/10 候选，主因是散件及替补被最高技能门槛排除。
- 修复后，原始练度在默认 10 分支下生成 10 个候选，全部完成 24 小时预热 + 72 小时采样，结果为 draft，最佳评分约 78850 分/日。
- 实际浏览器：导入原始 xlsx，默认 10 分支、seed 42、关闭可选深度微调，显示“一键排班成功”，应用到排班表，布局校验无冲突，应用后的收益重算也完成，按钮恢复可用。深度微调另行验证已进入第三阶段，未等待全部 20 次优化结束。
- 全员精零一级、没有但书/龙舌兰的池：完成 96 小时模拟；保留锁定主班；无普通主班/替补重复占用。
- 空池及极小池：明确返回人数不足，不虚构干员，不覆盖原排班。

## 检查记录

- 修改前：inventoryFallback 的 4 项回归失败（red.txt）。
- 全量扫描：114 个文件，1092 项通过、3 项失败、1 项跳过，另有 2 次测试进程通信超时（full-tests.txt）。运行期间源码更新，空池用例读取的是旧缓存；另两项失败为旧的“候选不足即拒绝”和“有限池必须失败”断言。
- 修正断言、空池保护及长同步测试的消息刷新后，最终复测受影响的 4 个文件：39/39 项通过、无通信错误（final-regression.txt）。覆盖 mixedLevelInventory、inventoryFallback、smartRosterParallel、WorkbenchShell。
- 其他组合、完整候选、串并行、252、长期模拟、计算与组件检查的结果保存在全量记录中。未把未完成模拟或测试工具超时作为成功测试。
- vue-tsc -b：通过。
- vite build：通过（build.txt），仅保留现有体积提示。
- git diff --check：通过。

## 行为边界

组合仍按原有技能条件准入，散件只使用实际解锁技能。人数不足以满足当前布局及独立替补时仍会明确阻止生成。此次更新源码与网页构建，未制作新的桌面发行包。
